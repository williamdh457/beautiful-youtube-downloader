# beautiful-youtube-downloader
Local-first YouTube batch downloader (Flask + yt-dlp): browse/pick channel videos with paging, add URLs, choose   formats, run parallel downloads, bilingual UI, uses bundled ffmpeg—no uploads, saves locally.
